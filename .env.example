# API Configuration
# Exchange rate API endpoint (e.g., https://api.exchangerate.host, https://api.exchangerate-api.com)
NEXT_PUBLIC_API_BASE_URL=https://api.exchangerate.host

# API Key (if required by your provider)
# Keep this secret - do not expose to client
API_KEY=your_api_key_here

# Optional: Server-side proxy mode
# Set to 'true' to use server-side proxy for API calls (recommended for securing API keys)
# Set to 'false' to make direct client-side calls (only if API is public)
USE_API_PROXY=true
